pipeline{
    agent any

    stages{
        stage('dependency install'){
            agent { docker { image 'node:14'}}
            steps{
                sh 'sudo chown -R 995:993 "/.npm"'
                sh 'npm install'
            }
        }
    }
}
