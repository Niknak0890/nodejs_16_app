pipeline{
    agent any

    stages{
        stage('dependency install'){
            agent { docker { image 'node'}}
            steps{
                sh 'npm install'
            }
        }
    }
}
